# CSV-to-JSON

JavaScript code for converting .csv file to JSON.

Use command "npm install" to install all modules listed as dependencies in package.json.

Use command "node csv2json".  

  
* Saves the converted data to file "customer-data.json".


